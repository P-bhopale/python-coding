1. Write a Python function to check whether a number is in a given range. 
def num_in_range(number,start,end):
    if(number <= start <= end):
        return True
    else:
        False
number = float(input("Enter a number: "))
start = float(input("Enter the start of the range: "))
end = float(input("Enter the end of the range: "))
    
if num_in_range(number,start,end):
    print(f"The number {number} is within range {start} to {end}. ")
else:
    print(f"The number {number} is not within range {start} to {end}. ")


2. Write a Python function that takes a number as a parameter and check the number is prime or not.  
number = int(input("Enter a number: "))
if(number <= 1):
    print(f"{number} is not a prime number.")
for i in range(2, number):
    if(number % i == 0):
        print(f"{number} is not a prime number.")
else:
    print(f"{number} is a prime number.")


3. Write a Python function that checks whether a passed string is palindrome or not
string = input("Enter a string: ")
if string == string[::-1]:
    print("The string is palindrome.")
else:
    print("The string is not a palindrome.")


5.Write a Python function to calculate the factorial of a number (non-negative integer). The function accepts the number as an argument.  
def factorial(num):
    if num < 0:
        return "Factorial is not defined for negatives numbers."
    result = 1

    for i in range(1,number + 1):
        result *= i

    return result
number = int(input("Enter a non-negative integer: "))

result = factorial(number)
print(result)

    
